import { {{pascalCase reducerEntity}}ActionTypes } from '../{{pascalCase reducerEntity}}ActionTypes';
//constants
import { updateObject } from '../../../constants/Helpers';
//interfaces
import { Action } from '../{{pascalCase reducerEntity}}ActionsInterfaces';
import { {{pascalCase name}}ReducerInitialState } from '../{{pascalCase reducerEntity}}ReducersInterfaces';

const initialState: {{pascalCase name}}ReducerInitialState = {
    testString: 'Initial test',
};

const reducer = (state = initialState, action: Action) => {
    switch (action.type) {
        case {{pascalCase reducerEntity}}ActionTypes.TEST_ACTION:
            return updateObject(state, { testString: 'Final test' });
        default:
            return state;
    }
};

export default reducer;
