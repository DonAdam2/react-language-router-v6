import { createSlice } from '@reduxjs/toolkit';
//interfaces
import { {{pascalCase name}}SliceInitialState } from '@/ts/store/{{camelCase reducerEntity}}/{{pascalCase reducerEntity}}EntityInterfaces';

const initialState: {{pascalCase name}}SliceInitialState = {
  testString: 'Initial test',
};

const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState,
  reducers: {
    updateTestString: (state) => {
      state.testString = 'Final test';
    },
  },
});

export const { updateTestString } = {{camelCase name}}Slice.actions;
export default {{camelCase name}}Slice.reducer;